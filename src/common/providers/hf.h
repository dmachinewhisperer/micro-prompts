#ifndef HF_H_
#define HF_H_
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "cJSON.h"
#include "llm-client.h"

/**
 * // API endpoint - replace MODEL_NAME with actual model name
const url = 'https://api-inference.huggingface.co/models/MODEL_NAME';

// Request body structure for text generation
const textRequest = {
  inputs: "Your prompt here",
  parameters: {
    max_new_tokens: 100,
    top_k: 50,
    top_p: 0.95,
    temperature: 0.7,
  }
};

Request body structure for prompting with image
const imageRequest = {
  inputs: {
    image: "BASE64_ENCODED_IMAGE" // or URL to image
  },
  parameters: {
    threshold: 0.9,
    top_k: 5
  }
};
 */

#endif